# 🚀 Инструкция по развертыванию на Vercel

## ✅ Что исправлено

1. **Удален конфликтующий файл** `scripts/set-sw-version.js` (оставлен только `.cjs`)
2. **Исправлен скрипт** `scripts/copy-photos.cjs` для совместимости с разными версиями Node.js
3. **Обновлен** `vercel.json` - API запросы теперь не перенаправляются на `index.html`

## 📋 Что нужно настроить на Vercel

### 1. Переменные окружения (Environment Variables)

В настройках проекта на Vercel добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
ADMIN_CHAT_ID=ваш_chat_id
```

**Как получить:**
- `TELEGRAM_BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
- `ADMIN_CHAT_ID` - узнайте у [@userinfobot](https://t.me/userinfobot)

### 2. Пошаговая инструкция

1. **Закоммитьте и запушьте изменения:**
   ```bash
   git add .
   git commit -m "fix: исправлена сборка для Vercel"
   git push origin main
   ```

2. **На Vercel:**
   - Перейдите в Settings → Environment Variables
   - Добавьте обе переменные
   - Нажмите "Redeploy" или дождитесь автоматического деплоя

3. **Проверка работы:**
   - Откройте ваше приложение
   - Попробуйте оформить заказ
   - Проверьте, что сообщение пришло в Telegram

## 🎯 Что работает на бесплатном плане Vercel

✅ **Статический Vite сайт** - без ограничений  
✅ **Serverless Functions** (ваш `api/send-order.ts`) - до 100GB часов в месяц  
✅ **Отправка в Telegram** - работает через API  
✅ **Фотографии** - кешируются и быстро загружаются  

## 🔧 Технические детали

### Структура проекта:
- `dist/` - собранный фронтенд (Vite)
- `api/send-order.ts` - Serverless Function для отправки заказов
- `photo/` - копируется в `dist/photo/` при сборке

### Как работает отправка заказов:
1. Пользователь оформляет заказ в приложении
2. Фронтенд отправляет POST запрос на `/api/send-order`
3. Vercel Serverless Function получает запрос
4. Function отправляет сообщение через Telegram Bot API
5. Вы получаете уведомление в Telegram

## ⚠️ Важно

- **Не коммитьте** файлы `.env` в Git
- Переменные окружения настраиваются **только в Vercel Dashboard**
- API endpoint: `https://ваш-домен.vercel.app/api/send-order`

## 🐛 Если что-то не работает

1. Проверьте логи в Vercel Dashboard → Deployments → Logs
2. Убедитесь, что переменные окружения установлены
3. Проверьте, что токен бота корректный
4. Убедитесь, что chat ID правильный (должен быть числом)
